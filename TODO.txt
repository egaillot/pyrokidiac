code:
- /!\ Kid notifies observers when _state changes_ (not just position)
- remove duplication of columns (6) + duplication of ground line (0.75 * height)
- use before to set JSDOM
- updateElementPosition -> refresh
- encapsulate code in views into a `init` method
- get initial states from models
- check scopes in source
-

features:
- buttons
- README
- favicon
